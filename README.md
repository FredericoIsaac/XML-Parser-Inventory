# XML-Parser-Inventory

The goal is parse truth XML file and get all their child's element like:

* productCode 
* productDescription
* productNumberCode 
* closingStockQuantity
* closingStockValue 

Then save info into a CSV file
